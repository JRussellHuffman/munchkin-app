// player 0
$( "#0.nameInput" ) .keyup(function() {
	var value = $( this ).val();
	$( "#0.nameText" ).text( value );
}).keyup();
// player 1
$( "#1.nameInput" ) .keyup(function() {
	var value = $( this ).val();
	$( "#1.nameText" ).text( value );
}).keyup();

// player 2
$( "#2.nameInput" ) .keyup(function() {
	var value = $( this ).val();
	$( "#2.nameText" ).text( value );
}).keyup();

// player 3
$( "#3.nameInput" ) .keyup(function() {
	var value = $( this ).val();
	$( "#3.nameText" ).text( value );
}).keyup();

// player 4
$( "#4.nameInput" ) .keyup(function() {
	var value = $( this ).val();
	$( "#4.nameText" ).text( value );
}).keyup();

// player 5
$( "#5.nameInput" ) .keyup(function() {
	var value = $( this ).val();
	$( "#5.nameText" ).text( value );
}).keyup();